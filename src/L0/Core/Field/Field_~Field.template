namespace Core
{
  template< typename Element, size_t L, size_t T >
  Field< Element, L, T >::~Field()
  {
    d_references->operator-=(1);
    if (d_references == 0)
    {
      delete   d_references;
      delete[] d_field;
      delete[] d_offsets;
    }
  }
}
