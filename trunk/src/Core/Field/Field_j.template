namespace Core
{
  template< typename Element, size_t L, size_t T >
  size_t Core::Field< Element, L, T >::moveBufferToData(size_t written)
  {
    std::cout << written << std::endl;

    return written;
  }
}
