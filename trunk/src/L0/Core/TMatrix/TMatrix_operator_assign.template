template< size_t T >
Core::TMatrix< T > &Core::TMatrix< T >::operator=(Core::TMatrix< T > const &other)
{
  if (d_transfer != other.d_transfer)
  {
    destroy();
    d_transfer = other.d_transfer;
    d_references = other.d_references;
    *d_references += d_references;
  }
  return *this;
}
