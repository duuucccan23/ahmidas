namespace Smearing
{
  inline APE::APE(double alpha)
    : d_alpha(alpha)
  {}
  
  inline void APE::smear(Fields::GaugeField &field, size_t iterations)
  {
    for (size_t ctr = 0; ctr < iterations; ++ctr)
      smear(field);
  }
}
