namespace Core
{
  template< typename Element, size_t L, size_t T >
  Field< Element, L, T >::Field()
  : d_bufferSize(0), d_buffer(0), d_grid(Core::Grid< L, T >::instance()),
    d_com(Core::Com< Element, L, T >()),
    d_field(new Element[d_grid.localVolume()])
  {
    std::fill(d_offsets, d_offsets + 4, 0);
  }
}